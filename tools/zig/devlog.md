## 2025-08-08

Possible approaches:
* Tokenize and Parse 
     
* No tokenize
  seems hacky and implementation will be harder
```zig
  pub fn parsePgn(reader) {
    while (!eof) {
        parseGame()
    }  
}
```
