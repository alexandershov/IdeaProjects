# Scheme

## What is it?
Scheme is a dialect of lisp


## Install
Chez scheme is a scheme implementation.

```shell
brew install chezscheme
```

## Usage
Continuations walkthrough: 
```shell
chez --script continuation.scm
```