# What is it?
Mercury is a mix of Prolog & Haskell. 
Essentially statically typed prolog with support of functional programming.

# Install
```shell
brew install mercury
```

# Usage

See [main.m](./main.m) for an example. Run it with:
```shell
mmc --make main && ./main
```